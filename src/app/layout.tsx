import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});

const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "LB2S Technology Corp",
  description:
    "L2BS Technology Corp est une entreprise innovante et polyvalente, créée par quatre jeunes associés passionnés et experts dans leurs domaines. Nous proposons une gamme complète de services techniques et de solutions sur mesure pour répondre aux besoins des entreprises, institutions et particuliers en Afrique de l’Ouest et au-delà.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
